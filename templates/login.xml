<fest:template xmlns:fest="http://fest.mail.ru">
    <fest:include src="./header.xml"/>
    <div class="row">
        <div class="col-md-4 col-md-offset-4 jumbotron" style="padding:10px">
            <form class="form-signin">
                <h2 class="form-signin-heading" style="text-align:center; margin-bottom:20px">Please sign in</h2>
                <label for="inputEmail" class="sr-only">Email address</label>
                <input type="email" id="inputEmail" class="form-control" placeholder="Email address" required="" autofocus=""/>
                <br/>
                <label for="inputPassword" class="sr-only">Password</label>
                <input type="password" id="inputPassword" class="form-control" placeholder="Password" required=""/>
                <div class="checkbox">
                    <label>
                        <input type="checkbox" value="remember-me"/>
                        Remember me
                    </label>
                </div>
                <button class="btn btn-success btn-block" type="submit">Sign in</button>
                <a type="button" class="btn btn-danger btn-block" style="margin-top:10px" href="#">Back</a>
            </form>
        </div>
    </div>
</fest:template>
