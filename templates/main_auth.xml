<fest:template xmlns:fest="http://fest.mail.ru" context_name="json">
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="container">
                <div class="navbar-header">
                    <a class="navbar-brand" href="#">
                        <i class="glyphicon glyphicon-knight"></i> iota game
                    </a>
                </div>
                <form class="navbar-form navbar-left">
                    <div class="form-group">
                        <div class="btn-group">
                            <button type="button" class="btn btn-link" data-toggle="button">
                                <i class="glyphicon glyphicon-paperclip"></i> Правила игры
                            </button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </nav>
    <div class="container">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                <div class="panel panel-default">
                    <div class="panel-body">
                        <a type="button" class="btn btn-primary btn-lg btn-block" href="#game"><i class="glyphicon glyphicon-play"></i> Начать игру</a>
                        <a type="button" class="btn btn-primary btn-lg btn-block" href="#scoreboard"><i class="glyphicon glyphicon-stats"></i> Просмотреть лучшие результаты</a>
                        <a type="button" class="btn btn-primary btn-lg btn-block" href="#logout"><i class="glyphicon glyphicon-log-out"></i> Выйти</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</fest:template>
